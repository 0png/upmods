import { EventEmitter } from 'node:events';

// Stub — full typed implementation added in T021/T023
// typed-emitter will be applied when implementing scanAndIdentify, checkUpdates, downloadUpdates
export class UpmodsCore extends EventEmitter {
  constructor() {
    super();
  }
}
