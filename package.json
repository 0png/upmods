{
  "name": "upmods",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Minecraft mods auto-updater CLI",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test:integration": "pnpm build && pnpm test:integration:built",
    "test:integration:built": "node scripts/integration-built-cli.mjs",
    "pack:cli": "node -e \"require('node:fs').mkdirSync('release', { recursive: true })\" && pnpm --dir packages/cli pack --pack-destination ../../release && node scripts/write-release-checksum.mjs",
    "release:check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm test:integration:built"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "eslint": "^9.21.0",
    "typescript-eslint": "^8.26.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
